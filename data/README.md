# Required data files

This directory intentionally does not contain the FASTA or mzML.gz shard.

Add these files manually before running the reviewed workflow:

```text
data/fasta/B2117_TARGET_PLUS_CRAP_GPM_CONTAMINANTS_V024.fasta
data/projection/MSB10955Pool_30MinDIA041023GPF_01.search_projection_shard_v26_1000spectra.mzML.gz
```

Do not commit RAW files or full mzML batches to this repository.
